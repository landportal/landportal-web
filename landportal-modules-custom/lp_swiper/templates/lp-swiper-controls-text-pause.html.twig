{#
/**
 * @file
 * Default theme implementation for a views swiper text pause control.
 *
 * Available variables:
 * - classes: Classes to apply to the control.
 * - start_text: Text to display while playing.
 * - vss_id: The swiper's id.
 *
 * @see template_preprocess_lp_swiper_controls_text_pause()
 *
 * @ingroup vss_templates
 */
#}
<span id="lp_swiper_controls_text_pause_{{ vss_id }}" {{ attributes.addClass(classes) }}>
  <a href="#">{{ start_text }}</a>
</span>
