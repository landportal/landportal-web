{#
/**
 * @file
 * Default theme implementation for a views swiper text previous control.
 *
 * Available variables:
 * - classes: Classes to apply to the control.
 * - vss_id: The swiper's id.
 *
 * @see template_preprocess_lp_swiper_controls_text_previous()
 *
 * @ingroup vss_templates
 */
#}
<span id="lp_swiper_controls_text_previous_{{ vss_id }}" {{ attributes.addClass(classes) }}>
  <a href="#">{{ 'Previous'|t }}</a>
</span>
