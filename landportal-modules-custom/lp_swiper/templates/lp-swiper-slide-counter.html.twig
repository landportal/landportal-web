{#
/**
 * @file
 * Default theme implementation for a views swiper slide counter.
 *
 * Available variables:
 * - classes: Classes to apply to the counter's div element.
 * - slide_count: Total number of slides.
 * - vss_id: The swiper's id.
 *
 * @see template_preprocess_lp_swiper_slide_counter()
 *
 * @ingroup vss_templates
 */
#}
<div id="lp_swiper_slide_counter_{{ vss_id }}" {{ attributes.addClass(classes) }}>
  <span class="num">1</span> {{ 'of'|t }} <span class="total">{{ slide_count }}</span>
</div>
