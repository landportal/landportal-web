{#
/**
* @file
* Landportal Issue library resources block's theme implementation.
*
* Available variables:
* - items:
*   - title: Issue library resources' title.
*   - url: Issue library resources' url.
*   - date: Issue library resources' publish date.
*   - image_url: Issue library resources' image url.
*   - image_title: Issue library resources' image title.
*   - image_alt: Issue library resources' image alt.
* - more_news_url: More Issue library resources page url.
*/
#}
<div>
  {% for item in items %}
    <div>
      <a href="{{ item.ss_url }}">
        <img src="{{ item['tm_field_image$file$url'].0 }}" title="{{ item['tm_field_image$file$field_file_image_title_text'].0 }}" alt="{{ item.image_alt }}">
      </a>
      <h3>
        <a href="{{ item.ss_url }}">{{ item.tm_title_field.0 }}</a>
      </h3>
      <div>{{ item.ds_field_doc_creation_date|date(format='j F Y') }}</div>
      <div>{{ item['tm_field_doc_description$value'].0|striptags|slice(0, 300) }}</div>
    </div>
  {% endfor %}
  <p><a href="{{ more_url }}">{{ more_label }}</a></p>
</div>
